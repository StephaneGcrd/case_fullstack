I would like to create an API based on FastAPI that will allow me to communicate or use my agent through a web interface that will be a React application, and for that I will use SSE.

The code needs to be very readable, documented, and explainable. This is not production code; it's more case study code for me to understand how it works.

This is an agent that is able to use two tools, which are query data and visualize. You can read that in the agent folder, and for my API and UI, I will need, of course, to be able to chat with the agent.

I will also need to display the thinking in the UI.

The tool calls should be visible, and the visualization should be visible as well.

The API, or the server events, need to have both the tool calls and the delta between the tools and generation. For instance, two goals if they have a delta should be sent as well. When a message is generated by the LLm, it should also send in a streaming way, word by word, so I will be able to display that in the front.

Do not code anything front end for now. This is a strictly separated API.

The output of the table is generated. If it's a plot-like figure, it generates an HTML snippet, and if it's a table, it generates a CSV. You can see that there are two examples in the output folder.
